manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
    # DoctorWangWang は不要になるので削除（残しても実害はないですが、整理のため）
  projects:
    - name: zmk
      remote: zmkfirmware
      revision: main
      import: app/west.yml
    # zmk-pmw3610-driver のブロックを丸ごと削除してください
      
  self:
    path: config
